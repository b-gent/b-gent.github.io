:orphan:

.. title:: MBEDTLS_TLS_LIBRARY

.. option:: CONFIG_MBEDTLS_TLS_LIBRARY

*Create mbed TLS transport layer security library*

Type: ``bool``

Help
====

Create the mbed SSL/TLS library in addition to the mbed crypto
library.

Direct dependencies
===================

\ :option:`NORDIC_SECURITY_BACKEND <CONFIG_NORDIC_SECURITY_BACKEND>`

*(Includes any dependencies from ifs and menus.)*

Defaults
========

No defaults. Implicitly defaults to ``n``.

Symbols selected by this symbol
===============================

- \ :option:`MBEDTLS_X509_LIBRARY <CONFIG_MBEDTLS_X509_LIBRARY>`

Kconfig definition
==================

.. highlight:: kconfig

At ``C:/Git/ncs/nrfxlib\nrf_security/Kconfig:42``

Included via ``C:/Git/ncs/nrfxlib/Kconfig.nrfxlib:14``

Menu path: (Top) → Nordic nrfxlib → Nordic Security → Use Nordic provided security backend

.. parsed-literal::

    config MBEDTLS_TLS_LIBRARY
    	bool "Create mbed TLS transport layer security library"
    	select \ :option:`MBEDTLS_X509_LIBRARY <CONFIG_MBEDTLS_X509_LIBRARY>`
    	depends on \ :option:`NORDIC_SECURITY_BACKEND <CONFIG_NORDIC_SECURITY_BACKEND>`
    	help
    	  Create the mbed SSL/TLS library in addition to the mbed crypto
    	  library.

*(The 'depends on' condition includes propagated dependencies from ifs and menus.)*