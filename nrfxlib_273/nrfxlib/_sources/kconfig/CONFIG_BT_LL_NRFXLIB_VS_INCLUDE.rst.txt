:orphan:

.. title:: BT_LL_NRFXLIB_VS_INCLUDE

.. option:: CONFIG_BT_LL_NRFXLIB_VS_INCLUDE

*Include nRF BLE Controller vendor specific HCI interface*

Type: ``bool``

Help
====

Include nRF BLE Controller vendor specific HCI interface.

Direct dependencies
===================

BT && BT_HCI

*(Includes any dependencies from ifs and menus.)*

Defaults
========

No defaults. Implicitly defaults to ``n``.

Kconfig definition
==================

.. highlight:: kconfig

At ``C:/Git/ncs/nrfxlib\ble_controller/Kconfig:13``

Included via ``C:/Git/ncs/nrfxlib/Kconfig.nrfxlib:11``

Menu path: (Top) → Nordic nrfxlib

.. parsed-literal::

    config BT_LL_NRFXLIB_VS_INCLUDE
    	bool "Include nRF BLE Controller vendor specific HCI interface"
    	depends on BT && BT_HCI
    	help
    	  Include nRF BLE Controller vendor specific HCI interface.

*(The 'depends on' condition includes propagated dependencies from ifs and menus.)*