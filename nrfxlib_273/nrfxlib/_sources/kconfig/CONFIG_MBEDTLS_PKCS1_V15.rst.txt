:orphan:

.. title:: MBEDTLS_PKCS1_V15

.. option:: CONFIG_MBEDTLS_PKCS1_V15

*PKCS1 v1.5 support*

Type: ``bool``

Direct dependencies
===================

\ :option:`MBEDTLS_RSA_C <CONFIG_MBEDTLS_RSA_C>` && \ :option:`MBEDTLS_RSA_C <CONFIG_MBEDTLS_RSA_C>` && \ :option:`NORDIC_SECURITY_BACKEND <CONFIG_NORDIC_SECURITY_BACKEND>`

*(Includes any dependencies from ifs and menus.)*

Defaults
========

No defaults. Implicitly defaults to ``n``.

Kconfig definition
==================

.. highlight:: kconfig

At ``C:/Git/ncs/nrfxlib\nrf_security/Kconfig:749``

Included via ``C:/Git/ncs/nrfxlib/Kconfig.nrfxlib:14``

Menu path: (Top) → Nordic nrfxlib → Nordic Security → Use Nordic provided security backend → RSA   - Rivest–Shamir–Adleman cryptosystem

.. parsed-literal::

    config MBEDTLS_PKCS1_V15
    	bool "PKCS1 v1.5 support"
    	depends on \ :option:`MBEDTLS_RSA_C <CONFIG_MBEDTLS_RSA_C>` && \ :option:`MBEDTLS_RSA_C <CONFIG_MBEDTLS_RSA_C>` && \ :option:`NORDIC_SECURITY_BACKEND <CONFIG_NORDIC_SECURITY_BACKEND>`

*(The 'depends on' condition includes propagated dependencies from ifs and menus.)*