:orphan:

.. title:: VANILLA_MBEDTLS_GCM_C

.. option:: CONFIG_VANILLA_MBEDTLS_GCM_C

*AES-GCM - AES Galois/Counter Mode support*

Type: ``bool``

Help
====

Enable the GCM module.
MBEDTLS_GCM_C setting in mbed TLS config file.

Direct dependencies
===================

(\ :option:`VANILLA_MBEDTLS_AES_C <CONFIG_VANILLA_MBEDTLS_AES_C>` || (\ :option:`MBEDTLS_VANILLA_BACKEND <CONFIG_MBEDTLS_VANILLA_BACKEND>` && \ :option:`MBEDTLS_AES_C <CONFIG_MBEDTLS_AES_C>` && !\ :option:`NRF_CRYPTO_BACKEND_COMBINATION_0 <CONFIG_NRF_CRYPTO_BACKEND_COMBINATION_0>`)) && (\ :option:`CC310_BACKEND <CONFIG_CC310_BACKEND>` || \ :option:`MBEDTLS_VANILLA_BACKEND <CONFIG_MBEDTLS_VANILLA_BACKEND>`) && \ :option:`NORDIC_SECURITY_BACKEND <CONFIG_NORDIC_SECURITY_BACKEND>`

*(Includes any dependencies from ifs and menus.)*

Default
=======

- y

Symbols selected by this symbol
===============================

- \ :option:`MBEDTLS_GCM_C <CONFIG_MBEDTLS_GCM_C>`

Kconfig definition
==================

.. highlight:: kconfig

At ``C:/Git/ncs/nrfxlib\nrf_security/Kconfig:413``

Included via ``C:/Git/ncs/nrfxlib/Kconfig.nrfxlib:14``

Menu path: (Top) → Nordic nrfxlib → Nordic Security → Use Nordic provided security backend → AEAD  - Authenticated Encryption with Associated Data

.. parsed-literal::

    config VANILLA_MBEDTLS_GCM_C
    	bool "AES-GCM - AES Galois/Counter Mode support"
    	default y
    	select \ :option:`MBEDTLS_GCM_C <CONFIG_MBEDTLS_GCM_C>`
    	depends on (\ :option:`VANILLA_MBEDTLS_AES_C <CONFIG_VANILLA_MBEDTLS_AES_C>` || (\ :option:`MBEDTLS_VANILLA_BACKEND <CONFIG_MBEDTLS_VANILLA_BACKEND>` && \ :option:`MBEDTLS_AES_C <CONFIG_MBEDTLS_AES_C>` && !\ :option:`NRF_CRYPTO_BACKEND_COMBINATION_0 <CONFIG_NRF_CRYPTO_BACKEND_COMBINATION_0>`)) && (\ :option:`CC310_BACKEND <CONFIG_CC310_BACKEND>` || \ :option:`MBEDTLS_VANILLA_BACKEND <CONFIG_MBEDTLS_VANILLA_BACKEND>`) && \ :option:`NORDIC_SECURITY_BACKEND <CONFIG_NORDIC_SECURITY_BACKEND>`
    	help
    	  Enable the GCM module.
    	  MBEDTLS_GCM_C setting in mbed TLS config file.

*(The 'depends on' condition includes propagated dependencies from ifs and menus.)*