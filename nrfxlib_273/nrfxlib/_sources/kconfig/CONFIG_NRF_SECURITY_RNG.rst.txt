:orphan:

.. title:: NRF_SECURITY_RNG

.. option:: CONFIG_NRF_SECURITY_RNG

*Random Number Generator support*

Type: ``bool``

Help
====

The Random Number Generator support in nRF Security provides a
Pseudorandom Number Generator, PRNG.
The Pseudorandom Number Generator is seeded by a True Random Number
Generator, TRNG, available in hardware.

Direct dependencies
===================

\ :option:`NORDIC_SECURITY_BACKEND <CONFIG_NORDIC_SECURITY_BACKEND>`

*(Includes any dependencies from ifs and menus.)*

Default
=======

- y

Symbols selected by this symbol
===============================

- \ :option:`MBEDTLS_AES_C <CONFIG_MBEDTLS_AES_C>`
- \ :option:`MBEDTLS_SHA256_C <CONFIG_MBEDTLS_SHA256_C>`

Kconfig definition
==================

.. highlight:: kconfig

At ``C:/Git/ncs/nrfxlib\nrf_security/Kconfig:150``

Included via ``C:/Git/ncs/nrfxlib/Kconfig.nrfxlib:14``

Menu path: (Top) → Nordic nrfxlib → Nordic Security → Use Nordic provided security backend

.. parsed-literal::

    config NRF_SECURITY_RNG
    	bool "Random Number Generator support"
    	default y
    	select \ :option:`MBEDTLS_AES_C <CONFIG_MBEDTLS_AES_C>`
    	select \ :option:`MBEDTLS_SHA256_C <CONFIG_MBEDTLS_SHA256_C>`
    	depends on \ :option:`NORDIC_SECURITY_BACKEND <CONFIG_NORDIC_SECURITY_BACKEND>`
    	help
    	  The Random Number Generator support in nRF Security provides a
    	  Pseudorandom Number Generator, PRNG.
    	  The Pseudorandom Number Generator is seeded by a True Random Number
    	  Generator, TRNG, available in hardware.

*(The 'depends on' condition includes propagated dependencies from ifs and menus.)*